// This file is kept for backward compatibility
// The actual Asset interface and initial data are now in AssetContext.tsx
// Re-export from context for convenience

export type { Asset } from '@/context/AssetContext';
